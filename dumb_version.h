#ifndef MANDELBROT_DUMB_VERSION_H
#define MANDELBROT_DUMB_VERSION_H

#include <stdint.h>

namespace dumb {
    void calc (unsigned int width, unsigned int height, uint32_t *counters);
}

#endif //MANDELBROT_DUMB_VERSION_H
